services:
  - type: web
    name: brd-agent
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.x
      - key: PORT
        value: 5000
      - key: GEMINI_API_KEY
        sync: false
